# REST API With Swagger Starter

This is a standalone backend showcase. Keep it small and polished: clean routes, clean OpenAPI docs, and predictable error responses.

## Run Later

```bash
npm install
npm run dev
```

Then open `http://localhost:4100/docs`.

